rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Temporary permissive rules for user migration
    // REMOVE AFTER MIGRATION IS COMPLETE
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
